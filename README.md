# Etsy Scraper

使用真实 Chrome 浏览器的 Etsy 商品图片爬虫，提供命令行和图形界面两种使用方式。

## ✨ 特性

- 🖼️ **图片下载**: 自动下载高清商品展示图
- 🎯 **图片选择**: 指定下载第几张图片（如只要首图）
- 🏷️ **标题过滤**: 从文件名中移除不需要的词汇
- 📂 **Section 批量**: 一键抓取店铺分类下所有商品
- 🔗 **多链接支持**: 一次处理多个商品或 Section
- 🔄 **断点续传**: 中断后可继续，自动跳过已完成商品
- 🛡️ **反爬虫**: 使用真实 Chrome + 手动验证，绕过 Captcha
- 🖥️ **GUI 界面**: 提供美观易用的桌面应用

## 📦 安装

### 前置要求

- Python 3.11+
- Poetry
- Google Chrome 浏览器

### 安装步骤

```bash
cd etsy_scraper

# 安装依赖
poetry install
```

## 🚀 使用方法

### 方式一：图形界面（推荐）

```bash
poetry run etsy-gui
```

启动后会打开一个美观的桌面应用，包含：
- **单商品抓取**: 输入商品链接，下载图片
- **Section 批量**: 输入 Section 链接，批量抓取
- **实时日志**: 查看抓取进度和状态
- **可视化配置**: 设置图片选择、标题过滤等选项

### 方式二：命令行

#### 单个商品抓取

```bash
# 基本用法
poetry run etsy-real "https://www.etsy.com/listing/123456789/product-name"

# 只下载第一张图
poetry run etsy-real "https://www.etsy.com/listing/123456789/..." -i 1

# 下载第 1、3、5 张图
poetry run etsy-real "https://www.etsy.com/listing/123456789/..." -i "1,3,5"

# 过滤标题中的词汇
poetry run etsy-real "https://www.etsy.com/listing/123456789/..." -f "Canvas,Poster,Wall Art"
```

#### 多个商品抓取

```bash
# 多个链接（空格分隔）
poetry run etsy-real "https://www.etsy.com/listing/111/..." "https://www.etsy.com/listing/222/..."

# 多链接 + 只下载首图
poetry run etsy-real "https://www.etsy.com/listing/111/..." "https://www.etsy.com/listing/222/..." -i 1
```

#### Section 批量抓取

```bash
# 基本用法 - 抓取整个 Section
poetry run etsy-section "https://www.etsy.com/shop/ShopName?section_id=12345"

# 只下载每个商品的第一张图
poetry run etsy-section "https://www.etsy.com/shop/ShopName?section_id=12345" -i 1

# 多个 Section
poetry run etsy-section "https://...?section_id=111" "https://...?section_id=222"
```

## 📦 打包为桌面应用

### 打包命令

```bash
# 打包为可执行文件
poetry run python build.py

# 仅清理构建文件
poetry run python build.py --clean
```

### 输出位置

- **macOS**: `dist/EtsyScraper.app`
- **Windows**: `dist/EtsyScraper/EtsyScraper.exe`

### 分发

将 `dist/EtsyScraper` 文件夹打包发送给用户即可，无需安装 Python。

## 📋 命令参考

### etsy-gui（图形界面）

```bash
poetry run etsy-gui
```

无需任何参数，直接启动图形界面。

### etsy-real（单/多商品）

| 参数 | 说明 | 示例 |
|------|------|------|
| `urls` | Etsy 商品 URL（支持多个） | `"https://..." "https://..."` |
| `-o, --output` | 输出目录（默认 `output`） | `-o my_images` |
| `-i, --images` | 下载哪些图片 | `-i 1` 或 `-i "1,3,5"` 或 `-i "2-4"` |
| `-f, --filter` | 标题过滤词 | `-f "Canvas,Poster"` |
| `-d, --delay` | 多链接间延迟秒数（默认 2） | `-d 3` |
| `-p, --port` | Chrome 调试端口（默认 9222） | `-p 9223` |

### etsy-section（单/多 Section）

| 参数 | 说明 | 示例 |
|------|------|------|
| `urls` | Section URL（支持多个） | `"https://...?section_id=X" "https://..."` |
| `-o, --output` | 输出目录（默认 `output`） | `-o my_images` |
| `-i, --images` | 下载哪些图片 | `-i 1` 或 `-i "1,3-5"` |
| `-f, --filter` | 标题过滤词 | `-f "Canvas,Poster"` |
| `-d, --delay` | 商品间延迟秒数（默认 2） | `-d 3` |
| `--section-delay` | Section 间延迟秒数（默认 3） | `--section-delay 5` |
| `--resume` | 启用断点续传（默认） | |
| `--no-resume` | 禁用断点续传 | |
| `--clear-progress` | 清理进度文件后退出 | |

## 📁 输出结构

### 单/多商品模式

```
output/
├── Product Title A-1.jpg
├── Product Title A-2.jpg
├── Product Title B-1.jpg
├── product_123456789_20260203_123456.json
└── product_987654321_20260203_123457.json
```

### Section 模式

```
output/
├── ShopName_12345/              # {店铺名}_{section_id}
│   ├── Product A-1.jpg
│   ├── Product A-2.jpg
│   └── .progress.json           # 断点续传进度文件（隐藏）
└── ShopName_67890/              # 多 Section 时会有多个目录
    ├── Product B-1.jpg
    └── .progress.json
```

## 🔧 工作流程

1. **启动 Chrome** - 程序启动真实 Chrome 浏览器（带调试端口）
2. **手动验证** - 如果出现 Captcha，在浏览器中手动完成验证
3. **确认继续** - 验证完成后，点击「继续抓取」按钮（GUI）或按 Enter（命令行）
4. **自动抓取** - 程序自动提取数据并下载图片

## ❓ FAQ

### 为什么使用这种方式？

Etsy 有非常强大的反爬虫系统（Captcha-Delivery），常规自动化方法几乎都会被拦截。这个工具的思路是：

- 使用**真实 Chrome**，不是自动化浏览器
- **手动完成验证**，不需要绕过
- 复用已验证的会话状态

### 遇到 Captcha 怎么办？

在打开的 Chrome 窗口中手动完成验证，然后回到应用点击「继续抓取」。

### 如何只下载首图？

- **GUI**: 在「图片选择」输入框填写 `1`
- **命令行**: 使用 `-i 1` 参数

### 文件名太长怎么办？

- **GUI**: 在「标题过滤」输入框填写要过滤的词汇，如 `Canvas,Poster,Wall Art`
- **命令行**: 使用 `-f "Canvas,Poster,Wall Art"` 参数

### 抓取中断了怎么办？

直接重新运行，程序会自动跳过已完成的商品（断点续传功能）。

### 如何打包给别人用？

运行打包命令后，将 `dist/EtsyScraper` 文件夹发送给用户：

```bash
poetry run python build.py
```

## 📄 License

MIT
